function AddComponent({ a, b }) {
  //   const a = obj.a;
  //   const b = obj.b;
  return (
    <div>
      <h1>Add Component</h1>
      <p>
        Add({a}, {b}) = {a + b}
      </p>
    </div>
  );
}

export default AddComponent;
